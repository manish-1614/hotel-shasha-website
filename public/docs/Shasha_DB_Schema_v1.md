<p><strong>SHASHA: PAUSED PERFECT</strong></p><p><em>Jibhi, Himachal Pradesh</em></p><p>  DATABASE SCHEMA DOCUMENT  ·  DB v1.0  </p><p><strong>Database Schema Document</strong></p><p><em>DDL · ERD · Indexes · Migrations · Seed Data</em></p><p>Version 1.0  ·  March 2026</p><p>Author: Manish — Senior Full-Stack Architect</p><p>Repository: github.com/manish-1614/hotel-shasha-website</p><h1><strong>1. Overview &amp; Design Decisions</strong></h1><p>This document specifies the complete PostgreSQL database schema for the Shasha Jibhi platform. The database is introduced in Phase 2 using NeonDB (serverless PostgreSQL) with Drizzle ORM for type-safe queries. Phase 1 operates without a database — all enquiry data flows through email only.</p><table><thead><tr><th><p><strong>Attribute</strong></p></th><th><p><strong>Value</strong></p></th></tr></thead><tbody><tr><td><p>Engine</p></td><td><p>PostgreSQL 16 (NeonDB serverless)</p></td></tr><tr><td><p>ORM</p></td><td><p>Drizzle ORM v0.30+ — schema-first, type-safe, no runtime overhead</p></td></tr><tr><td><p>Connection</p></td><td><p>Neon serverless driver (@neondatabase/serverless) with connection pooling</p></td></tr><tr><td><p>Migrations</p></td><td><p>Drizzle Kit — drizzle-kit generate:pg + drizzle-kit push:pg</p></td></tr><tr><td><p>Schema file</p></td><td><p>src/db/schema.ts (single file, all tables)</p></td></tr><tr><td><p>Naming convention</p></td><td><p>snake_case for tables and columns · plural table names · id as primary key</p></td></tr><tr><td><p>Primary keys</p></td><td><p>UUID v7 (time-ordered) via gen_random_uuid() — prefixed for readability (enq_, room_, etc.)</p></td></tr><tr><td><p>Timestamps</p></td><td><p>created_at + updated_at on all tables · auto-managed via Drizzle timestamps()</p></td></tr><tr><td><p>Soft deletes</p></td><td><p>deleted_at timestamp where applicable — no hard deletes on user-facing records</p></td></tr><tr><td><p>Data residency</p></td><td><p>NeonDB us-east-1 region (Phase 2 may migrate to Singapore for latency)</p></td></tr></tbody></table><p>⚠  The database is NOT used in Phase 1. All schema and DDL in this document is forward-looking for Phase 2 implementation. The schema is designed to be extensible — Phase 3 tables are documented but not deployed until needed.</p><h1><strong>2. Entity Relationship Diagram (Text)</strong></h1><p>The following ASCII ERD shows all tables, their primary/foreign key relationships, and cardinality for Phase 2 scope.</p><p>┌───────────────────┐       ┌───────────────────┐<br />│     rooms         │       │   rate_configs     │<br />│─────────────────  │       │─────────────────── │<br />│ PK id             │──────►│ FK room_type (enum)│<br />│    slug           │  1:N  │    season          │<br />│    name           │       │    price_room_only │<br />│    type (enum)    │       │    price_cp_person │<br />│    capacity       │       │    valid_from      │<br />│    description    │       │    valid_to        │<br />│    features[]     │       └───────────────────┘<br />│    price_base     │<br />│    is_active      │       ┌───────────────────┐<br />│    sort_order     │       │   blocked_dates   │<br />└──────────┬────────┘       │────────────────── │<br />           │                │ PK id             │<br />           │ 1:N            │ FK room_id        │◄─── rooms.id<br />           │                │    date_from      │<br />           ▼                │    date_to        │<br />┌───────────────────┐       │    reason         │<br />│    enquiries      │       └───────────────────┘<br />│─────────────────  │<br />│ PK id             │       ┌───────────────────┐<br />│    name           │       │  gallery_images   │<br />│    email          │       │───────────────────│<br />│    phone          │       │ PK id             │<br />│    city           │       │    cloudinary_id  │<br />│    room_type(enum)│       │    category(enum) │<br />│ FK room_id        │───────│    alt_text       │<br />│    checkin        │  N:1  │    featured       │<br />│    checkout       │       │    sort_order     │<br />│    nights(computed│       │    width          │<br />│    guests         │       │    height         │<br />│    meal_plan(enum)│       └───────────────────┘<br />│    requests       │<br />│    source(enum)   │       ┌───────────────────┐<br />│    status(enum)   │       │   testimonials    │<br />│    est_revenue    │       │───────────────────│<br />│    host_notes     │       │ PK id             │<br />│    ip_hash        │       │    guest_name     │<br />│    created_at     │       │    stay_type      │<br />│    updated_at     │       │    quote          │<br />└───────────────────┘       │    rating         │<br />                            │    source         │<br />┌───────────────────┐       │    is_published   │<br />│  admin_users      │       │    created_at     │<br />│───────────────────│       └───────────────────┘<br />│ PK id             │<br />│    email          │<br />│    password_hash  │<br />│    role           │<br />│    last_login_at  │<br />│    created_at     │<br />└───────────────────┘</p><h1><strong>3. Table Schemas — Full DDL</strong></h1><h2><strong>3.1 enquiries</strong></h2><p>The primary data table. Every successfully processed contact form submission creates one record. This is the source of truth for all guest leads.</p><p>-- enquiries table DDL<br />CREATE TABLE enquiries (<br />  id              TEXT        PRIMARY KEY DEFAULT 'enq_' || replace(gen_random_uuid()::text, '-', ''),<br />  -- Guest personal details<br />  name            VARCHAR(80)  NOT NULL,<br />  email           VARCHAR(255) NOT NULL,<br />  phone           VARCHAR(15)  NOT NULL,<br />  city            VARCHAR(60),<br />  -- Stay preferences<br />  room_type       room_type_enum NOT NULL,<br />  room_id         TEXT         REFERENCES rooms(id),<br />  checkin         DATE         NOT NULL,<br />  checkout        DATE         NOT NULL,<br />  nights          INTEGER      GENERATED ALWAYS AS (checkout - checkin) STORED,<br />  guests          SMALLINT     NOT NULL CHECK (guests BETWEEN 1 AND 8),<br />  meal_plan       meal_plan_enum NOT NULL,<br />  requests        TEXT         CHECK (char_length(requests) &lt;= 400),<br />  source          source_enum,<br />  -- Computed financials<br />  estimated_revenue INTEGER,   -- in INR, calculated at insert time<br />  -- CRM workflow<br />  status          enquiry_status_enum NOT NULL DEFAULT 'new',<br />  host_notes      TEXT,        -- internal notes from host<br />  -- Privacy &amp; audit<br />  ip_hash         VARCHAR(64), -- SHA-256 of IP — for dedup analytics only<br />  -- Timestamps<br />  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  deleted_at      TIMESTAMPTZ             -- soft delete<br />);<br /><br />-- Enum definitions<br />CREATE TYPE room_type_enum AS ENUM (<br />  'duplex-triple', 'duplex-quad', 'private', 'dorm'<br />);<br />CREATE TYPE meal_plan_enum AS ENUM (<br />  'room-only', 'cp-plan', 'full-board'<br />);<br />CREATE TYPE source_enum AS ENUM (<br />  'instagram', 'google', 'friend', 'blog', 'ota', 'other'<br />);<br />CREATE TYPE enquiry_status_enum AS ENUM (<br />  'new', 'contacted', 'confirmed', 'cancelled', 'completed'<br />);</p><h3><strong>enquiries — Column Reference</strong></h3><table><thead><tr><th><p><strong>Column</strong></p></th><th><p><strong>Type</strong></p></th><th><p><strong>Nullable</strong></p></th><th><p><strong>Default</strong></p></th><th><p><strong>Description</strong></p></th></tr></thead><tbody><tr><td><p>id</p></td><td><p>TEXT</p></td><td><p>No</p></td><td><p>'enq_' + UUID</p></td><td><p>Primary key with readable prefix</p></td></tr><tr><td><p>name</p></td><td><p>VARCHAR(80)</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Guest full name</p></td></tr><tr><td><p>email</p></td><td><p>VARCHAR(255)</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Guest email — used for dedup and communication</p></td></tr><tr><td><p>phone</p></td><td><p>VARCHAR(15)</p></td><td><p>No</p></td><td><p>—</p></td><td><p>10-digit Indian mobile</p></td></tr><tr><td><p>city</p></td><td><p>VARCHAR(60)</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>Guest home city — for personalisation</p></td></tr><tr><td><p>room_type</p></td><td><p>room_type_enum</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Room type selected in form Step 2</p></td></tr><tr><td><p>room_id</p></td><td><p>TEXT (FK)</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>FK to rooms.id (nullable — room types may not all be in DB yet)</p></td></tr><tr><td><p>checkin</p></td><td><p>DATE</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Requested check-in date</p></td></tr><tr><td><p>checkout</p></td><td><p>DATE</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Requested check-out date</p></td></tr><tr><td><p>nights</p></td><td><p>INTEGER</p></td><td><p>No</p></td><td><p>Generated</p></td><td><p>Computed: checkout − checkin. Stored for query performance.</p></td></tr><tr><td><p>guests</p></td><td><p>SMALLINT</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Number of guests (1–8)</p></td></tr><tr><td><p>meal_plan</p></td><td><p>meal_plan_enum</p></td><td><p>No</p></td><td><p>—</p></td><td><p>Meal plan selection</p></td></tr><tr><td><p>requests</p></td><td><p>TEXT</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>Free-text special requests. Max 400 chars enforced by check.</p></td></tr><tr><td><p>source</p></td><td><p>source_enum</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>How guest discovered Shasha</p></td></tr><tr><td><p>estimated_revenue</p></td><td><p>INTEGER</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>Calculated INR estimate at submission time</p></td></tr><tr><td><p>status</p></td><td><p>enquiry_status_enum</p></td><td><p>No</p></td><td><p>'new'</p></td><td><p>CRM workflow status — updated by admin</p></td></tr><tr><td><p>host_notes</p></td><td><p>TEXT</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>Internal host notes — never shown to guest</p></td></tr><tr><td><p>ip_hash</p></td><td><p>VARCHAR(64)</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>SHA-256(IP) — for abuse analytics. Not the raw IP.</p></td></tr><tr><td><p>created_at</p></td><td><p>TIMESTAMPTZ</p></td><td><p>No</p></td><td><p>now()</p></td><td><p>Submission timestamp</p></td></tr><tr><td><p>updated_at</p></td><td><p>TIMESTAMPTZ</p></td><td><p>No</p></td><td><p>now()</p></td><td><p>Last update timestamp</p></td></tr><tr><td><p>deleted_at</p></td><td><p>TIMESTAMPTZ</p></td><td><p>Yes</p></td><td><p>NULL</p></td><td><p>Soft delete — NULL means active</p></td></tr></tbody></table><h2><strong>3.2 rooms</strong></h2><p>Static room catalog. In Phase 1, room data lives in src/data/rooms.ts. Phase 2 migrates this to the database for admin-managed updates without code deployments.</p><p>CREATE TABLE rooms (<br />  id          TEXT        PRIMARY KEY DEFAULT 'room_' || replace(gen_random_uuid()::text, '-', ''),<br />  slug        VARCHAR(60)  NOT NULL UNIQUE, -- URL slug: duplex-cottage, private-room, dorm<br />  name        VARCHAR(100) NOT NULL,<br />  type        room_type_enum NOT NULL,<br />  capacity    SMALLINT     NOT NULL,        -- max guests<br />  description TEXT,<br />  features    TEXT[]       NOT NULL DEFAULT '{}', -- e.g. {'Skylight roof','Valley view'}<br />  price_base  INTEGER      NOT NULL,        -- base price room-only in INR<br />  is_active   BOOLEAN      NOT NULL DEFAULT true,<br />  sort_order  SMALLINT     NOT NULL DEFAULT 0,<br />  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()<br />);</p><h2><strong>3.3 rate_configs</strong></h2><p>Seasonal pricing configurations. Supports multiple active rate tiers (standard, peak, off-season) with date ranges. The application selects the most specific active rate for a given checkin date.</p><p>CREATE TYPE rate_season_enum AS ENUM ('standard', 'peak', 'offseason');<br /><br />CREATE TABLE rate_configs (<br />  id                  TEXT        PRIMARY KEY DEFAULT gen_random_uuid()::text,<br />  room_type           room_type_enum NOT NULL,<br />  season              rate_season_enum NOT NULL DEFAULT 'standard',<br />  price_room_only     INTEGER      NOT NULL,        -- INR per night<br />  price_cp_per_person INTEGER      NOT NULL,        -- INR per person per night (CP plan)<br />  valid_from          DATE         NOT NULL,<br />  valid_to            DATE         NOT NULL,<br />  is_active           BOOLEAN      NOT NULL DEFAULT true,<br />  created_at          TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  updated_at          TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  CONSTRAINT valid_date_range CHECK (valid_to &gt; valid_from)<br />);</p><h2><strong>3.4 gallery_images</strong></h2><p>Managed image catalog. In Phase 1, this is a static TypeScript file (imageManifest.ts). Phase 2 persists it here for admin-managed ordering, alt text, and featured image selection.</p><p>CREATE TYPE image_category_enum AS ENUM (<br />  'food', 'rooms-interior', 'rooms-lifestyle', 'exterior-drone', 'exterior-ground'<br />);<br /><br />CREATE TABLE gallery_images (<br />  id             TEXT        PRIMARY KEY DEFAULT gen_random_uuid()::text,<br />  cloudinary_id  VARCHAR(255) NOT NULL UNIQUE, -- e.g. shasha/food/2Y4A8137<br />  category       image_category_enum NOT NULL,<br />  alt_text       VARCHAR(255) NOT NULL,<br />  featured       BOOLEAN      NOT NULL DEFAULT false,<br />  sort_order     SMALLINT     NOT NULL DEFAULT 0,<br />  width          SMALLINT,    -- original pixel width (for aspect ratio)<br />  height         SMALLINT,    -- original pixel height<br />  is_active      BOOLEAN      NOT NULL DEFAULT true,<br />  created_at     TIMESTAMPTZ NOT NULL DEFAULT now()<br />);</p><h2><strong>3.5 testimonials</strong></h2><p>CREATE TYPE testimonial_source_enum AS ENUM ('direct', 'google', 'booking', 'instagram');<br /><br />CREATE TABLE testimonials (<br />  id            TEXT        PRIMARY KEY DEFAULT gen_random_uuid()::text,<br />  guest_name    VARCHAR(80)  NOT NULL,<br />  stay_type     room_type_enum,              -- which room they stayed in<br />  quote         TEXT         NOT NULL CHECK (char_length(quote) &lt;= 500),<br />  rating        SMALLINT     NOT NULL CHECK (rating BETWEEN 1 AND 5),<br />  source        testimonial_source_enum NOT NULL DEFAULT 'direct',<br />  is_published  BOOLEAN      NOT NULL DEFAULT false, -- must be explicitly published<br />  created_at    TIMESTAMPTZ NOT NULL DEFAULT now()<br />);</p><h2><strong>3.6 blocked_dates  (Phase 3)</strong></h2><p>CREATE TABLE blocked_dates (<br />  id          TEXT        PRIMARY KEY DEFAULT gen_random_uuid()::text,<br />  room_id     TEXT         NOT NULL REFERENCES rooms(id) ON DELETE CASCADE,<br />  date_from   DATE         NOT NULL,<br />  date_to     DATE         NOT NULL,<br />  reason      VARCHAR(255),                -- 'Maintenance', 'Personal stay', etc.<br />  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  CONSTRAINT valid_block_range CHECK (date_to &gt;= date_from)<br />);</p><h2><strong>3.7 admin_users  (Phase 2)</strong></h2><p>CREATE TYPE admin_role_enum AS ENUM ('super_admin', 'host');<br /><br />CREATE TABLE admin_users (<br />  id              TEXT        PRIMARY KEY DEFAULT gen_random_uuid()::text,<br />  email           VARCHAR(255) NOT NULL UNIQUE,<br />  password_hash   VARCHAR(255) NOT NULL, -- bcrypt, cost factor 12<br />  role            admin_role_enum NOT NULL DEFAULT 'host',<br />  last_login_at   TIMESTAMPTZ,<br />  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),<br />  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()<br />);<br /><br />-- Seed: initial admin users (run once during Phase 2 setup)<br />-- Passwords set via admin setup script — never stored in codebase</p><h1><strong>4. Indexes</strong></h1><p>All indexes are documented with their purpose and the queries they optimise. Over-indexing on a small dataset is avoided — only indexes that serve real query patterns are created.</p><p>-- enquiries table indexes<br />CREATE INDEX idx_enquiries_email<br />  ON enquiries(email)<br />  WHERE deleted_at IS NULL;<br />-- Purpose: Deduplication lookup by email + checkin<br /><br />CREATE INDEX idx_enquiries_checkin<br />  ON enquiries(checkin)<br />  WHERE deleted_at IS NULL;<br />-- Purpose: Filter enquiries by date range in admin dashboard<br /><br />CREATE INDEX idx_enquiries_status<br />  ON enquiries(status)<br />  WHERE deleted_at IS NULL;<br />-- Purpose: Filter by status (new/contacted/confirmed)<br /><br />CREATE INDEX idx_enquiries_created_at<br />  ON enquiries(created_at DESC)<br />  WHERE deleted_at IS NULL;<br />-- Purpose: Default sort in admin dashboard (newest first)<br /><br />CREATE UNIQUE INDEX idx_enquiries_dedup<br />  ON enquiries(email, checkin)<br />  WHERE deleted_at IS NULL;<br />-- Purpose: DB-level deduplication constraint (belt + suspenders with Redis dedup)<br /><br />-- rooms table indexes<br />CREATE UNIQUE INDEX idx_rooms_slug ON rooms(slug) WHERE is_active = true;<br />-- Purpose: URL slug lookup: /stay/duplex-cottage → rooms WHERE slug='duplex-cottage'<br /><br />-- rate_configs table indexes<br />CREATE INDEX idx_rate_configs_room_type_dates<br />  ON rate_configs(room_type, valid_from, valid_to)<br />  WHERE is_active = true;<br />-- Purpose: Find applicable rate for a given room + checkin date<br /><br />-- gallery_images table indexes<br />CREATE INDEX idx_gallery_images_category<br />  ON gallery_images(category, sort_order)<br />  WHERE is_active = true;<br />-- Purpose: Gallery filter by category with sort order<br /><br />CREATE INDEX idx_gallery_images_featured<br />  ON gallery_images(featured)<br />  WHERE featured = true AND is_active = true;<br />-- Purpose: Fetch featured images for homepage / room pages<br /><br />-- testimonials table indexes<br />CREATE INDEX idx_testimonials_published<br />  ON testimonials(is_published, created_at DESC)<br />  WHERE is_published = true;<br />-- Purpose: Fetch published testimonials for homepage carousel</p><h1><strong>5. Drizzle ORM Schema — src/db/schema.ts</strong></h1><p>The Drizzle schema is the single source of truth for the database in the application. Raw DDL above is for documentation and review — the actual tables are managed via Drizzle Kit migrations.</p><p>import { pgTable, text, varchar, smallint, integer, boolean,<br />         date, timestamp, pgEnum, check, uniqueIndex, index } from 'drizzle-orm/pg-core'<br />import { sql } from 'drizzle-orm'<br /><br />// ── Enums ────────────────────────────────────────────────────────────<br />export const roomTypeEnum = pgEnum('room_type_enum', [<br />  'duplex-triple', 'duplex-quad', 'private', 'dorm'<br />])<br />export const mealPlanEnum = pgEnum('meal_plan_enum', [<br />  'room-only', 'cp-plan', 'full-board'<br />])<br />export const sourceEnum = pgEnum('source_enum', [<br />  'instagram', 'google', 'friend', 'blog', 'ota', 'other'<br />])<br />export const enquiryStatusEnum = pgEnum('enquiry_status_enum', [<br />  'new', 'contacted', 'confirmed', 'cancelled', 'completed'<br />])<br />export const imageCategoryEnum = pgEnum('image_category_enum', [<br />  'food', 'rooms-interior', 'rooms-lifestyle', 'exterior-drone', 'exterior-ground'<br />])<br /><br />// ── enquiries ────────────────────────────────────────────────────────<br />export const enquiries = pgTable('enquiries', {<br />  id:               text('id').primaryKey().$defaultFn(() =&gt; 'enq_' + crypto.randomUUID().replace(/-/g,'')),<br />  name:             varchar('name',    { length: 80  }).notNull(),<br />  email:            varchar('email',   { length: 255 }).notNull(),<br />  phone:            varchar('phone',   { length: 15  }).notNull(),<br />  city:             varchar('city',    { length: 60  }),<br />  roomType:         roomTypeEnum('room_type').notNull(),<br />  roomId:           text('room_id').references(() =&gt; rooms.id),<br />  checkin:          date('checkin').notNull(),<br />  checkout:         date('checkout').notNull(),<br />  guests:           smallint('guests').notNull(),<br />  mealPlan:         mealPlanEnum('meal_plan').notNull(),<br />  requests:         text('requests'),<br />  source:           sourceEnum('source'),<br />  estimatedRevenue: integer('estimated_revenue'),<br />  status:           enquiryStatusEnum('status').notNull().default('new'),<br />  hostNotes:        text('host_notes'),<br />  ipHash:           varchar('ip_hash', { length: 64 }),<br />  createdAt:        timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),<br />  updatedAt:        timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),<br />  deletedAt:        timestamp('deleted_at', { withTimezone: true }),<br />}, (t) =&gt; ({<br />  emailIdx:    index('idx_enquiries_email').on(t.email),<br />  checkinIdx:  index('idx_enquiries_checkin').on(t.checkin),<br />  statusIdx:   index('idx_enquiries_status').on(t.status),<br />  dedupIdx:    uniqueIndex('idx_enquiries_dedup').on(t.email, t.checkin),<br />}))<br /><br />// ── rooms ────────────────────────────────────────────────────────────<br />export const rooms = pgTable('rooms', {<br />  id:          text('id').primaryKey().$defaultFn(() =&gt; 'room_' + crypto.randomUUID().replace(/-/g,'')),<br />  slug:        varchar('slug', { length: 60 }).notNull().unique(),<br />  name:        varchar('name', { length: 100 }).notNull(),<br />  type:        roomTypeEnum('type').notNull(),<br />  capacity:    smallint('capacity').notNull(),<br />  description: text('description'),<br />  features:    text('features').array().notNull().default(sql`'{}'::text[]`),<br />  priceBase:   integer('price_base').notNull(),<br />  isActive:    boolean('is_active').notNull().default(true),<br />  sortOrder:   smallint('sort_order').notNull().default(0),<br />  createdAt:   timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),<br />  updatedAt:   timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),<br />})<br /><br />// ── Type exports (used throughout application) ────────────────────<br />export type Enquiry     = typeof enquiries.$inferSelect<br />export type NewEnquiry  = typeof enquiries.$inferInsert<br />export type Room        = typeof rooms.$inferSelect</p><h1><strong>6. Migration Strategy</strong></h1><h2><strong>6.1 Drizzle Kit Configuration</strong></h2><p>// drizzle.config.ts<br />import type { Config } from 'drizzle-kit'<br /><br />export default {<br />  schema:  './src/db/schema.ts',<br />  out:     './drizzle',            // migration files directory<br />  driver:  'pg',<br />  dbCredentials: {<br />    connectionString: process.env.DATABASE_URL!,<br />  },<br />  breakingChanges: false,          // Warn on destructive changes<br />  verbose: true,<br />} satisfies Config</p><h2><strong>6.2 Migration Commands</strong></h2><p># Generate migration files from schema changes<br />pnpm drizzle-kit generate:pg<br /><br /># Review generated SQL before applying<br />cat drizzle/0001_*.sql<br /><br /># Apply migrations to NeonDB<br />pnpm drizzle-kit push:pg<br /><br /># Open Drizzle Studio (visual DB browser)<br />pnpm drizzle-kit studio</p><h2><strong>6.3 Phase 2 Initial Migration — 0001_initial_schema.sql</strong></h2><p>This is the first migration file that establishes the complete Phase 2 schema. Generated by Drizzle Kit from schema.ts.</p><p>-- drizzle/0001_initial_schema.sql<br />-- Generated by Drizzle Kit — do not edit manually<br /><br />-- Create enums first (must exist before tables reference them)<br />CREATE TYPE "room_type_enum"         AS ENUM ('duplex-triple','duplex-quad','private','dorm');<br />CREATE TYPE "meal_plan_enum"         AS ENUM ('room-only','cp-plan','full-board');<br />CREATE TYPE "source_enum"            AS ENUM ('instagram','google','friend','blog','ota','other');<br />CREATE TYPE "enquiry_status_enum"    AS ENUM ('new','contacted','confirmed','cancelled','completed');<br />CREATE TYPE "image_category_enum"    AS ENUM ('food','rooms-interior','rooms-lifestyle','exterior-drone','exterior-ground');<br />CREATE TYPE "testimonial_source_enum" AS ENUM ('direct','google','booking','instagram');<br />CREATE TYPE "admin_role_enum"        AS ENUM ('super_admin','host');<br />CREATE TYPE "rate_season_enum"       AS ENUM ('standard','peak','offseason');<br /><br />-- Create tables (rooms before enquiries for FK reference)<br />CREATE TABLE rooms ( ... );   -- full DDL from Section 3.2<br />CREATE TABLE enquiries ( ... ); -- full DDL from Section 3.1<br />CREATE TABLE rate_configs ( ... );<br />CREATE TABLE gallery_images ( ... );<br />CREATE TABLE testimonials ( ... );<br />CREATE TABLE admin_users ( ... );<br /><br />-- Create indexes<br />CREATE INDEX idx_enquiries_email ON enquiries(email) WHERE deleted_at IS NULL;<br />-- ... all indexes from Section 4<br /><br />-- updated_at auto-update trigger<br />CREATE OR REPLACE FUNCTION update_updated_at()<br />RETURNS TRIGGER AS $$<br />BEGIN NEW.updated_at = now(); RETURN NEW; END;<br />$$ LANGUAGE plpgsql;<br /><br />CREATE TRIGGER enquiries_updated_at BEFORE UPDATE ON enquiries<br />  FOR EACH ROW EXECUTE FUNCTION update_updated_at();<br />CREATE TRIGGER rooms_updated_at BEFORE UPDATE ON rooms<br />  FOR EACH ROW EXECUTE FUNCTION update_updated_at();</p><h1><strong>7. Seed Data</strong></h1><p>Seed data populates the database after initial migration. Run once in Phase 2 setup. File: src/db/seed.ts</p><p>// src/db/seed.ts — run with: pnpm tsx src/db/seed.ts<br />import { db } from './index'<br />import { rooms, rate_configs, testimonials } from './schema'<br /><br />async function seed() {<br />  // ── Rooms ──────────────────────────────────────────────────────<br />  await db.insert(rooms).values([<br />    {<br />      slug: 'duplex-cottage',<br />      name: 'Duplex Cottage',<br />      type: 'duplex-triple',<br />      capacity: 3,<br />      description: 'Skylight stargazing, infinite glass windows, cedar interiors.',<br />      features: ['Skylight roof','18-ft glass window','Low-sitting sun area','Cedar wood flooring'],<br />      priceBase: 4800,<br />      sortOrder: 1,<br />    },<br />    {<br />      slug: 'duplex-cottage-quad',<br />      name: 'Duplex Cottage — Quad',<br />      type: 'duplex-quad',<br />      capacity: 4,<br />      description: 'Same stunning duplex experience for groups of four.',<br />      features: ['Skylight roof','18-ft glass window','Cedar wood flooring'],<br />      priceBase: 6000,<br />      sortOrder: 2,<br />    },<br />    {<br />      slug: 'private-room',<br />      name: 'Private Room',<br />      type: 'private',<br />      capacity: 2,<br />      description: 'Cozy double-sharing rooms with valley views and sofa-cum-bed.',<br />      features: ['Valley view','Sofa-cum-bed','Cheerful decor','Snug blankets'],<br />      priceBase: 2000,<br />      sortOrder: 3,<br />    },<br />    {<br />      slug: 'unisex-dorm',<br />      name: 'Unisex Dormitory',<br />      type: 'dorm',<br />      capacity: 6,<br />      description: '6-bed luxury wooden bunk dorm with attic library and mountain view.',<br />      features: ['Premium bunk beds','Attic library','Indoor games','Himalayan view'],<br />      priceBase: 700,<br />      sortOrder: 4,<br />    },<br />  ]).onConflictDoNothing()<br /><br />  // ── Rate Configs (standard season, full year 2026) ────────────<br />  await db.insert(rate_configs).values([<br />    { roomType: 'duplex-triple', season: 'standard', priceRoomOnly: 4800, priceCpPerPerson: 1800, validFrom: '2026-01-01', validTo: '2026-12-31' },<br />    { roomType: 'duplex-quad',   season: 'standard', priceRoomOnly: 6000, priceCpPerPerson: 1750, validFrom: '2026-01-01', validTo: '2026-12-31' },<br />    { roomType: 'private',       season: 'standard', priceRoomOnly: 2000, priceCpPerPerson: 1300, validFrom: '2026-01-01', validTo: '2026-12-31' },<br />    { roomType: 'dorm',          season: 'standard', priceRoomOnly: 700,  priceCpPerPerson: 0,    validFrom: '2026-01-01', validTo: '2026-12-31' },<br />  ]).onConflictDoNothing()<br /><br />  console.log('Seed complete.')<br />  process.exit(0)<br />}<br /><br />seed().catch(e =&gt; { console.error(e); process.exit(1) })</p><h1><strong>8. Common Drizzle Query Patterns</strong></h1><p>These are the most-used query patterns in the application. All are type-safe and inferred from the Drizzle schema.</p><h2><strong>8.1 Insert new enquiry (POST /api/contact)</strong></h2><p>import { db } from '@/db'<br />import { enquiries, type NewEnquiry } from '@/db/schema'<br />import { calculateEstimate } from '@/lib/pricing'<br /><br />async function insertEnquiry(data: FormData, ipHash: string) {<br />  const estimatedRevenue = calculateEstimate({<br />    roomType: data.roomType, nights: data.nights,<br />    guests:   data.guests,   mealPlan: data.mealPlan,<br />  })<br /><br />  const newEnquiry: NewEnquiry = {<br />    ...data,<br />    estimatedRevenue: estimatedRevenue.min,<br />    ipHash,<br />    status: 'new',<br />  }<br /><br />  const [inserted] = await db.insert(enquiries).values(newEnquiry).returning()<br />  return inserted<br />}</p><h2><strong>8.2 List enquiries for admin dashboard</strong></h2><p>import { db } from '@/db'<br />import { enquiries } from '@/db/schema'<br />import { eq, desc, gte, lte, like, and, isNull } from 'drizzle-orm'<br /><br />async function listEnquiries({ page=1, limit=20, status, from, to, q }) {<br />  const offset = (page - 1) * limit<br /><br />  const conditions = [isNull(enquiries.deletedAt)]<br />  if (status &amp;&amp; status !== 'all') conditions.push(eq(enquiries.status, status))<br />  if (from) conditions.push(gte(enquiries.checkin, from))<br />  if (to)   conditions.push(lte(enquiries.checkin, to))<br />  if (q)    conditions.push(like(enquiries.name, `%${q}%`))<br /><br />  const [data, [{ count }]] = await Promise.all([<br />    db.select().from(enquiries)<br />      .where(and(...conditions))<br />      .orderBy(desc(enquiries.createdAt))<br />      .limit(limit).offset(offset),<br />    db.select({ count: sql&lt;number&gt;`count(*)` })<br />      .from(enquiries).where(and(...conditions)),<br />  ])<br /><br />  return { data, pagination: { page, limit, total: count, totalPages: Math.ceil(count/limit) } }<br />}</p><h2><strong>8.3 Get applicable rate for a checkin date</strong></h2><p>import { db } from '@/db'<br />import { rate_configs } from '@/db/schema'<br />import { eq, lte, gte, and } from 'drizzle-orm'<br /><br />async function getRate(roomType: RoomTypeEnum, checkinDate: string) {<br />  const [rate] = await db<br />    .select()<br />    .from(rate_configs)<br />    .where(and(<br />      eq(rate_configs.roomType, roomType),<br />      eq(rate_configs.isActive, true),<br />      lte(rate_configs.validFrom, checkinDate),<br />      gte(rate_configs.validTo,   checkinDate),<br />    ))<br />    .limit(1)<br /><br />  return rate ?? null<br />}</p><p><em>End of Database Schema Document</em></p><p>Shasha: Paused Perfect  ·  DB Schema v1.0  ·  March 2026</p>